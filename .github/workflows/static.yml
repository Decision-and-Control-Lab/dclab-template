# Kept as a manual compatibility entry for existing bookmarks.
# Automatic builds and deployments are handled by build-template-gallery.yml.
name: Legacy static deployment (manual notice)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Use the template gallery workflow
        run: echo "Run Build LaTeX template gallery instead; this legacy workflow no longer deploys."
